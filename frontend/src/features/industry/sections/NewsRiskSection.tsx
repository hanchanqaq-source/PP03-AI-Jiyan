import { ExternalLink } from "lucide-react";
import type { IndustryNewsItem, IndustryReportTemplate } from "../types";
import { EmptyEvidence, ReportSection, TruthBadge } from "./shared";

export function NewsRiskSection({ template, news = [] }: { template: IndustryReportTemplate; news?: IndustryNewsItem[] }) {
  return <ReportSection id="news-risk" index="08" title="新闻、催化与风险" eyebrow="Catalysts and risks"><div className="grid gap-7 lg:grid-cols-2"><div><h3 className="mb-3 text-sm font-semibold">最近重要新闻</h3>{news.length ? <div className="space-y-2">{news.slice(0, 8).map((item) => <a key={`${item.url}-${item.title}`} href={item.url} target="_blank" rel="noreferrer" className="block border-b border-border/40 py-2 text-sm hover:text-primary"><span>{item.title}</span><span className="mt-1 flex items-center gap-2 text-[10px] text-muted-foreground">{item.source} · {item.time}<ExternalLink className="h-3 w-3" /></span></a>)}</div> : <EmptyEvidence>资讯接口为空或尚未刷新。</EmptyEvidence>}</div><div className="space-y-5"><div><div className="mb-2 flex items-center gap-2"><h3 className="text-sm font-semibold">未来催化观察</h3><TruthBadge status="development" /></div><ul className="space-y-2 text-sm text-muted-foreground">{template.catalysts.map((item) => <li key={item} className="border-l-2 border-primary/50 pl-3">{item}</li>)}</ul></div><div><h3 className="mb-2 text-sm font-semibold text-warning">主要风险</h3><ul className="space-y-2 text-sm text-muted-foreground">{template.risks.map((item) => <li key={item}>• {item}</li>)}</ul></div><div><h3 className="mb-2 text-sm font-semibold">反向信号</h3><ul className="space-y-2 text-sm text-muted-foreground">{template.reverseSignals.map((item) => <li key={item}>• {item}</li>)}</ul></div></div></div></ReportSection>;
}
