# PP03 V0.2-W1 浏览器验收记录

验收日期：2026-08-16（Asia/Shanghai）

页面：`http://127.0.0.1:5173/portfolio-analysis`

数据目录规范：`D:\AI_Workspace\04-Projects\PP03-AI-Jiyan\.tmp\acceptance\v0.2-w1`（仅在验收执行期间通过 `VR_DATA_DIR` 使用，不是用户默认持仓目录；收口时自动清理）

## 已完成交互

### 第二轮高密度看板（schema v3）

- 以 1440×1000 桌面视口核验标题栏、四张蓝黑渐变总览卡、独立数据状态栏、筛选/排序工具栏及一行一基金的六列表格；三只基金可在一屏内完整显示。
- 主列表只显示基金/代码、持有金额/占比、今日估算、累计盈亏、官方净值/日期和操作；用户快照、参考市值、份额来源、缓存来源等审计信息只在详情抽屉中显示。
- 通过浏览器新增 `000001`、`519029` 两只公开基金，与已有公开测试基金 `017811` 组成三基金组合；快速模式默认只显示当前持有金额，累计盈亏、买入日期、备注、标签与精确模式位于“其他信息”折叠区。
- 浏览器编辑 `519029`：金额从 ¥15,000 更新为 ¥16,000，累计盈亏从 -¥800 更新为 -¥700；组合总值相应从 ¥45,000 更新为 ¥46,000。
- 六类筛选实测结果：今日上涨 1、今日下跌 1、盈利 2、亏损 1、数据待补充 0；六种排序均可选择，按基金名称排序首行为 `017811 东方人工智能主题混合C`。
- 详情抽屉继续展示用户快照、正式净值参考值、盘中估算参考值、前十大持仓、行业暴露和数据质量；1月、3月、6月、1年、3年、全部六档均实际切换并保持选中状态。
- 组合区显示 7 只重合证券、逐基金持有关系、组合估算暴露、一级行业进度条及未知部分。
- 页面内删除二次确认弹窗通过打开与取消路径验收，没有触发原生 `confirm`。三条隔离公开测试持仓随后经本地 API 清空，用于核验单一空状态面板。
- 最终修复后再次通过内置浏览器核验“数据说明”弹窗：打开后焦点进入关闭按钮，按 Escape 关闭，焦点回到触发按钮；空状态刷新后未残留失败提示。
- 新版页面控制台 `error/warning` 均为 0；未访问登录页、浏览器账号、Cookie 或站点存储。

### 第一轮真实数据闭环

- 空持仓状态和手动降级入口。
- `000001` 代码搜索与“华夏成长”名称搜索；同码股票、指数结果不会作为基金显示。
- 选择基金、填写份额/单位成本/买入日期/备注/完整标签并保存。
- 编辑持仓后刷新页面和重启后端，用户真值仍存在。
- 重复添加被阻止并显示明确提示。
- 打开基金详情五个区域；切换 1月、3月、6月、1年、3年、全部六个净值范围。
- 查看 2026-Q2 前十大持仓、覆盖率、非实时披露说明、行业暴露、系统标签/用户标签和数据质量。
- 三基金组合显示 7 只重复证券、逐基金权重、组合暴露和行业已识别/未知比例。
- 单基金刷新加载态、正常缓存、后端完全不可用失败态、上游不可用时过期缓存降级态。
- 新浏览器标签控制台：0 个 error/warn；页面恢复正常服务后再次核验。
- 用户在动作发生前确认删除隔离验收持仓 `519029 华夏稳增混合`；删除后文件、API 和浏览器刷新均只剩 `000001`、`000021`，组合仍显示 7 只重复证券。
- 原生 `window.confirm` 在内置浏览器自动化中会阻塞；已改为可访问的页面内二次确认弹窗，并覆盖取消、确认两条测试路径。
- 路径卫生收口已确认临时目录仅含公开测试基金、空股票持仓文件和验收缓存；收口后自动删除全部临时数据，未保留用户持仓或未提交代码。

## 截图索引

| 文件 | 证据 |
|---|---|
| `01-empty-portfolio.jpg` | 初始空状态 |
| `02-add-holding-flow.jpg` | 基金添加流程 |
| `03-portfolio-analysis.jpg` | 组合概览和持仓 |
| `04-fund-detail-nav.jpg` | 基金详情与净值 |
| `05-holdings-data-quality.jpg` | 前十大持仓和行业暴露 |
| `06-data-quality.jpg` | 数据质量区域 |
| `07-portfolio-overlap.jpg` | 多基金重复持仓 |
| `08-industry-concentration.jpg` | 组合行业集中度与未知比例 |
| `09-clean-search-console.jpg` | 修正后的 `000001` 唯一基金搜索结果 |
| `10-refresh-loading.jpg` | 单基金刷新加载态 |
| `11-backend-failure-state.jpg` | 后端失败时的显式错误和空数字 |
| `12-stale-cache-state.jpg` | 过期缓存显式标记 |
| `13-final-main-screen.jpg` | 最终正常联网页面主屏 |
| `16-full-page-final.jpg` | 由浏览器滚动分段自动拼接的完整页面截图 |
| `17-delete-confirmation-complete.jpg` | 删除 `519029` 后仅剩两条隔离持仓，组合重合度仍为 7 |
| `18-round2-add-drawer.png` | 第二轮添加基金抽屉：识别结果、核心金额和折叠其他信息 |
| `19-round2-overview.png` | 1440×1000 新版总览：四卡、状态栏、三基金高密度列表与组合区 |
| `20-round2-fund-list.png` | 三基金列表、红涨绿跌、组合重合度和行业集中度 |
| `21-round2-fund-detail.png` | 基金详情抽屉、净值曲线和三类估值信息 |
| `22-round2-empty-state.png` | 隔离测试持仓清空后的单一空状态面板 |

## 自动化验证摘要

- 后端离线：153 passed，13 deselected；持仓 schema v3、双模式、迁移与估值回归均通过。
- 后端完整集：首次 165 passed、1 条港股现金流 live 测试因上游瞬时返回空对象失败；该单项立即复跑通过，失败路径不属于持仓分析模块。
- 前端 Vitest：15 个文件、47 个测试全部通过；新增覆盖刷新原子性、失败提示恢复、删除后分析刷新失败及弹窗焦点管理。
- 原有前端兼容测试：16 passed。
- 生产构建：通过；主入口 654.12 kB（gzip 190.43 kB），保留包体积告警。

删除测试持仓属于破坏性浏览器操作。本次已在动作发生前取得用户对 `519029 华夏稳增混合` 的明确确认；路径卫生收口随后删除全部隔离验收临时数据，未触碰用户默认持仓目录、项目源码或任何登录账号。后续验收只能使用上述 D 盘 `.tmp` 路径，并在验收结束后自动清理。
