# V0.2-W3 Task 0 — implementation preflight evidence

Date: 2026-08-25 (Asia/Shanghai)
Status: `PASS_TASK0_PREFLIGHT_COMPLETE` — the initial Windows launcher gap was recovered with the documented approved bundled Python 3.12 interpreter; no production-code change was needed.

## Authorization and scope

```
AGENTS_W3_RESTRICTION_OVERRIDDEN_FOR_THIS_WORK=YES
AUTHORITY=LATEST_EXPLICIT_USER_INSTRUCTION
SCOPE=V0.2-W3_ONLY
```

Task 0 only prepared the implementation environment and recorded fixed-Head baseline evidence. It did not perform Tasks 1–10 and did not modify production business code.

## Approved revision and worktree protection

Implementation worktree:

```
path: D:\AI_Workspace\AI_Projects\PP03-AI-Jiyan-worktrees\codex-pp03-v0.2-w3-industry-research-impl
branch: codex/pp03-v0.2-w3-industry-research-impl
HEAD: 68801a7f439451b377d16ae8b105180f443ffb69
subject: docs(pp03): plan v0.2-w3 industry research
clean before Task 0 evidence: YES
```

Protected A2-W1 worktree (read-only audit only):

```
path: D:\AI_Workspace\AI_Projects\PP03-AI-Jiyan-worktrees\codex-pp03-a2-multi-source-provider-hub
branch: codex/pp03-a2-multi-source-provider-hub
HEAD: 377535e3d8e37976a57be6541f9e0e0d5c114a22
clean: YES
modified: NO
```

Configured remotes:

```
origin: https://github.com/hanchanqaq-source/PP03-AI-Jiyan.git
upstream: https://github.com/simonlin1212/Vibe-Research.git
```

Read-only remote check:

```text
git --no-optional-locks -c safe.directory=<implementation-worktree> -C <implementation-worktree> ls-remote origin refs/heads/codex/pp03-v0.2-w3-industry-research-impl refs/heads/codex/pp03-v0.2-w3-industry-research
exit: 0
68801a7f439451b377d16ae8b105180f443ffb69 refs/heads/codex/pp03-v0.2-w3-industry-research
```

The approved planning branch matched the fixed approved Head. No remote implementation-branch ref was returned; no fetch, push, merge, or remote write was performed.

## Toolchain and dependency inputs

```text
node --version
exit: 0
v24.18.0

npm --version
exit: 0
11.16.0

py -3.12 -c "import sys; assert sys.version_info[:2] == (3, 12)"
exit: 103
No suitable Python runtime found
```

Read-only launcher diagnosis:

```text
py -0p
exit: 0
-V:3.11 * C:\Users\26365\AppData\Local\Programs\Python\Python311\python.exe
```

No Python 3.12 interpreter was registered in the Windows `py` launcher. Task 0 deliberately did not substitute Python 3.11. The continuation ruling approved the verified bundled Python 3.12 interpreter as the project-local, system-nonmutating equivalent.

```text
py -3.12 -m venv backend\.venv                         NOT_RUN: launcher has no registered 3.12
```

SHA-256 dependency-input hashes:

```text
backend\requirements.txt=77FFBEEACCF60C5FACB6B179025AC8C1FD36A9CEFA00B4C256BA379490E8057D
backend\requirements-dev.txt=178062B78F2796A94403FA38F5C6986507FC77A14222B13DAFE5008CC6C9A27B
frontend\package-lock.json=3B5D77FE477432EA6D4AC2D33B098942C64EB5B4B71C373BD71DBE03B5C356EF
```

Frontend locked install:

```text
cwd: frontend
command: npm ci
exit: 0
result: added 333 packages; audited 334 packages
reported advisories: 4 high severity
reported pending install-script review: esbuild@0.25.12
```

The advisory and pending script-review notices were recorded only. No dependency upgrade, lockfile change, or `npm audit fix` was performed.

## Isolated acceptance environment

Ignored reusable bootstrap (not tracked):

```text
.superpowers\sdd\2026-08-25-pp03-v0.2-w3-industry-research-plan\task-0-env.ps1
```

It creates and validates this absolute root before every test/application subprocess:

```text
D:\AI_Workspace\AI_Projects\PP03-AI-Jiyan-worktrees\codex-pp03-v0.2-w3-industry-research-impl\.tmp\acceptance\v0.2-w3
```

All paths were created and asserted under that root:

```text
TEMP=...\temp                 UNDER_ROOT=True
TMP=...\tmp                   UNDER_ROOT=True
VR_DATA_DIR=...\data          UNDER_ROOT=True
VR_REPORTS_DIR=...\reports    UNDER_ROOT=True
VR_NEWS_CACHE_DIR=...\news    UNDER_ROOT=True
VR_EVIDENCE_DIR=...\evidence  UNDER_ROOT=True
VR_ACCEPTANCE_DIR=...\acceptance UNDER_ROOT=True
VR_LOG_DIR=...\logs           UNDER_ROOT=True
```

The bootstrap removes process-scoped environment names matching credential-like categories (`API_KEY`, `ACCESS_KEY`, `TOKEN`, `SECRET`, `PASSWORD`, `PASSWD`, `CREDENTIAL`, `AUTH`/`AUTHORIZATION`) without recording values. At the validation invocation, matching names present/cleared were: `none`. It then set:

```text
VR_ALLOW_PAID_PROVIDER_TESTS=0
VR_OFFLINE=1
VR_SOURCE_HEALTH_STARTUP=0
```

No credential values were read, logged, or retained. The `npm` cache was also set to `<root>\npm-cache` for the dependency installation.

## Fixed-Head offline baseline

Every test/build command below dot-sourced the bootstrap in its parent PowerShell process before invoking the child process.

| Area | Command (cwd) | Exit | Fresh result |
|---|---|---:|---|
| Backend offline | `backend\.venv\Scripts\python.exe -m pytest -m "not live" -q` (`backend`) | 0 | 2412 passed, 28 skipped, 13 deselected, 1 warning, 130.52s. |
| Frontend main | `npm run test:run` (`frontend`) | 0 | 31 files passed; 459 tests passed. |
| Frontend legacy | `npm run test:legacy` (`frontend`) | 0 | 16 tests passed; 0 failed/skipped/todo. |
| Production build | `npm run build` (`frontend`) | 0 | TypeScript build and Vite production build completed. |

Build emitted a size warning for an approximately 868 kB minified application chunk and generated an empty `vendor-charts` chunk; neither condition failed the command.

## Python 3.12 continuation and completed backend baseline

The continuation used the authorized absolute interpreter below, after dot-sourcing the same ignored bootstrap before each command:

```text
C:\Users\26365\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe
```

Provenance and version checks:

```text
<bundled-python> --version
exit: 0
Python 3.12.13

<bundled-python> -c "import sys, venv; assert sys.version_info[:2] == (3, 12)"
exit: 0
venv import: successful

<bundled-python> -m venv backend\.venv
exit: 0

backend\.venv\Scripts\python.exe -c "import sys; assert sys.version_info[:2] == (3, 12)"
exit: 0
3.12.13 (MSC v.1944 64 bit)
```

The required project-local dependency actions then completed with the same requirement hashes recorded above:

```text
backend\.venv\Scripts\python.exe -m pip install -r backend\requirements.txt
final confirmation exit: 0
backend\.venv\Scripts\python.exe -m pip check
exit: 0 (No broken requirements found)

backend\.venv\Scripts\python.exe -m pip install -r backend\requirements-dev.txt
exit: 0
backend\.venv\Scripts\python.exe -m pip check
exit: 0 (No broken requirements found)
```

The first runtime-install invocation continued in its owned child process after its captured tool output ended. A concurrent confirmation invocation therefore encountered Windows `WinError 32` on a pandas file. Read-only process evidence identified the still-running owned installer; after it exited naturally, the exact runtime-install command was rerun and completed with exit 0. No package version pin, production file, credential, or system installation was changed.

The first full non-Live test invocation progressed to 91% but was stopped after its owned pytest process remained idle. A bounded isolated reproduction captured the worker stack in `cache_management._scan_owned_files`: the initial bootstrap value `VR_ACCEPTANCE_DIR=<root>` made the cache scan traverse the intentionally isolated npm/pip caches also located below `<root>`. The ignored bootstrap was narrowed to `VR_ACCEPTANCE_DIR=<root>\acceptance`, an empty descendant; `TEMP`, `TMP`, data, reports, news, evidence, logs, and this acceptance subdirectory were freshly revalidated as paths under the unchanged absolute Task 0 root. This changed only ignored environment bootstrap behavior, not production business code or the root boundary.

Fresh final command and result:

```text
cwd: backend
backend\.venv\Scripts\python.exe -m pytest -m "not live" -q
exit: 0
2412 passed, 28 skipped, 13 deselected, 1 warning in 130.52s
```

The single warning is a Starlette deprecation warning concerning its TestClient/httpx combination. It did not fail the baseline. No Live test, paid-provider test, app service, or credential use occurred.

## Completed hard gate

Task 0 now has the required isolated Python 3.12 virtual environment, runtime and development requirements, frontend lockfile installation, backend non-Live baseline, frontend main and legacy tests, and production build evidence. The implementation work may proceed only within the separately authorized V0.2-W3 scope; this record neither starts Tasks 1–10 nor alters release/merge authority.
